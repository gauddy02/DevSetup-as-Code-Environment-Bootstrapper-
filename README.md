# DevSetup-as-Code-Environment-Bootstrapper-
CLI tool that installs and configures a full dev environment from a YAML file.  Core Features
....
evsetup init → generates config file

Installs language runtimes (Node, Python, Go)

Sets up git hooks, linting, VSCode settings

Cross-platform support
....
